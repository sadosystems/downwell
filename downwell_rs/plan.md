In the folder ~/source/downwell/decompiled_downwell we have the pre extracted assets and decompiled source code from downwell. in the folder ~/sources/geist we have a tool currently hard wired to this same version of down well that allows you to diff the framebuffer output of  2 binaries given the same series of inputs deterministically. like I said it is already wired up for down well and I even added an example of a downwell copy where a single pixel is changed (in the falling welltaro sprite) so you can demo how the tool works. your goal is to write a functionally identical port of the original downwell targeting in rust you have the assets the original gml code and the geist oracle to help you in this mission. 

word vommit high level goals: "no engine" write the game directly using only systems the game requires. DOD, casey muratori / john blow style use AOS use simple POD minimal abstraction no malloc do not bother with lifetimes at all basically write crust (https://github.com/tsoding/Crust) only the rules of my crust are VERY minimal dependencies ideally none, no malloc or free ever no doing stuff that ends up calling malloc or free, only use references when you MUST, generally user raw pointers. use statically allocated arenas for like everything that needs it. everything gets a fixed size up front. generally like crust meets tigerstyle but slightly more lax. the final game should run much faster than the GM game... if it doesnt something went wrong.  