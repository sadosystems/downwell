xx = x;
yy = y;

if (x > (room_width / 2))
    xsp = random_range(-1, 0);
else
    xsp = random_range(0, 1);

ysp = random_range(-2.8, -3);
angleAdd = random_range(-1, 1);
ugrav = 0.1;
image_speed = 0.2;
dFlash = 1;
